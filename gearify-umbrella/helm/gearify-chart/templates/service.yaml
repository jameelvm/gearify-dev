apiVersion: v1
kind: Service
metadata:
  name: {{ include "gearify.fullname" . }}
  labels:
    app: {{ include "gearify.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: {{ include "gearify.name" . }}
